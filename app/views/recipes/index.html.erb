<h1 class="text-xl sm:text-3xl font-bold mb-8 text-purple-700 text-center bg-white/80 px-4 py-2 inline-block rounded shadow mx-auto">
  🍳 レシピ一覧
</h1>

<div class="grid gap-6 sm:grid-cols-2 md:grid-cols-3 max-w-6xl mx-auto">
  <% @recipes.each do |recipe| %>
    <div class="bg-yellow-50 shadow-xl rounded-2xl overflow-hidden transition-transform hover:scale-105">
      <%= link_to recipe_path(recipe), class: "block" do %>
        <!-- 📸 画像 -->
        <% if recipe.image.attached? %>
          <%= image_tag recipe.image, class: "w-full" %>
        <% else %>
          <div class="w-full h-40 bg-gray-200 flex items-center justify-center text-gray-500">
            画像なし
          </div>
         <% end %>

        <!-- 📋 内容 -->
        <div class="p-4">
          <h2 class="text-xl font-bold text-purple-800 mb-2 truncate"><%= recipe.title %></h2>
          <p class="text-gray-700 text-sm mb-2 line-clamp-2 whitespace-pre-wrap">
            <%= recipe.ingredients %>
          </p>
          
          <!-- 🕒 所要時間やタグを表示したい場合 -->
          <% if recipe.required_time.present? %>
            <p class="text-sm text-gray-500">⏱️ 約 <%= recipe.required_time %> 分</p>
          <% end %>

          <% if recipe.tags.any? %>
            <div class="mt-2 flex flex-wrap gap-1">
              <% recipe.tags.each do |tag| %>
                <span class="bg-purple-100 text-purple-600 text-xs px-2 py-1 rounded-full">#<%= tag.name %></span>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
