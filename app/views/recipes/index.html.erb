<h1 class="text-xl sm:text-3xl font-bold mb-8 text-orange-600 text-center bg-orange-100 px-4 py-2 inline-block rounded shadow mx-auto font-sans">
  🍳 レシピ一覧
</h1>

<div class="grid gap-6 sm:grid-cols-2 md:grid-cols-3 max-w-6xl mx-auto">
  <% @recipes.each do |recipe| %>
    <div class="bg-orange-50 shadow-xl rounded-2xl overflow-hidden transition-transform hover:scale-105">
      <%= link_to recipe_path(recipe), class: "block" do %>
        <% if recipe.image.attached? %>
          <%= image_tag recipe.image, class: "w-full" %>
        <% else %>
          <div class="w-full h-40 bg-gray-200 flex items-center justify-center text-gray-500">
            画像なし
          </div>
         <% end %>

        <div class="p-4">
          <h2 class="text-xl font-sans font-bold text-amber-700 mb-2 truncate"><%= recipe.title %></h2>
          
          <p class="text-gray-700 text-sm mb-2 line-clamp-2 whitespace-pre-wrap">
            <%= recipe.ingredients %>
          </p>

          <% if recipe.required_time.present? %>
            <p class="text-sm text-gray-500">⏱️ 約 <%= recipe.required_time %> 分</p>
          <% end %>

          <% if recipe.tags.any? %>
            <div class="mt-2 flex flex-wrap gap-1">
              <% recipe.tags.each do |tag| %>
                <span class="bg-orange-100 text-orange-700 text-xs px-2 py-1 rounded-full">#<%= tag.name %></span>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
