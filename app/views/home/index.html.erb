<div class="text-center py-16 bg-white shadow rounded">
  <h1 class="text-4xl font-bold text-purple-700">ようこそ、Family Recipeへ</h1>
  <p class="mt-4 text-lg text-gray-700">
    家族だけでレシピを共有する、あたたかな思い出のアルバムのようなアプリです。
  </p>

  <% if user_signed_in? %>
    <div class="mt-8 space-x-4">
      <%= link_to "レシピ一覧を見る", recipes_path, class: "bg-purple-600 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to "レシピを投稿する", new_recipe_path, class: "bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded" %>
    </div>
  <% else %>
    <div class="mt-8 space-x-4">
      <%= link_to "ログイン", new_user_session_path, class: "bg-purple-500 hover:bg-purple-600 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to "新規登録", new_user_registration_path, class: "bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded" %>
    </div>
  <% end %>
</div>