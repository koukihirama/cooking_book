<div class="text-center py-24 px-4 bg-yellow-50 shadow-xl rounded-2xl max-w-3xl mx-auto mt-16">
  <h1 class="text-4xl sm:text-5xl font-bold text-purple-700 mb-10">
    👨‍👩‍👧‍👦 ようこそ、Family Recipeへ
  </h1>
  
  <p class="text-lg sm:text-xl text-gray-700 leading-loose mb-16">
    家族だけでレシピを共有する、<br>
    あたたかな思い出のアルバムのようなアプリです。
  </p>

  <% if user_signed_in? %>
    <div class="flex flex-col sm:flex-row justify-center gap-4">
      <%= link_to "📖 レシピ一覧を見る", recipes_path, class: "bg-purple-600 hover:bg-purple-700 text-white text-lg font-semibold py-3 px-6 rounded-full transition" %>
      <%= link_to "✍️ レシピを投稿する", new_recipe_path, class: "bg-green-500 hover:bg-green-600 text-white text-lg font-semibold py-3 px-6 rounded-full transition" %>
    </div>
  <% else %>
    <div class="flex flex-col sm:flex-row justify-center gap-4">
      <%= link_to "🔑 ログイン", new_user_session_path, class: "bg-purple-500 hover:bg-purple-600 text-white text-lg font-semibold py-3 px-6 rounded-full transition" %>
      <%= link_to "📝 新規登録", new_user_registration_path, class: "bg-blue-500 hover:bg-blue-600 text-white text-lg font-semibold py-3 px-6 rounded-full transition" %>
    </div>
  <% end %>
</div>