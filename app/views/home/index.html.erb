<div class="text-center p-6">
  <h1 class="text-3xl font-bold text-purple-700">ようこそ、家族のレシピ帳へ</h1>
  <p class="mt-2 text-gray-600">家族の思い出をレシピにして残そう</p>

  <% if user_signed_in? %>
    <%= link_to "レシピ一覧を見る", recipes_path, class: "btn btn-primary mt-4" %>
  <% else %>
    <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline mt-4 mr-2" %>
    <%= link_to "新規登録", new_user_registration_path, class: "btn btn-primary mt-4" %>
  <% end %>
</div>

<div class="mt-10">
  <h2 class="text-xl font-semibold mb-4">新着レシピ</h2>
  <div class="grid gap-4 sm:grid-cols-2">
    <% @recent_recipes.each do |recipe| %>
      <div class="bg-white shadow rounded-lg overflow-hidden">
        <%= link_to recipe_path(recipe) do %>
          <%= image_tag recipe.photo.variant(resize_to_limit: [400, 200]) if recipe.photo.attached? %>
          <div class="p-4">
            <h3 class="text-lg font-bold"><%= recipe.title %></h3>
            <p class="text-sm text-gray-600 truncate"><%= recipe.ingredients %></p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>